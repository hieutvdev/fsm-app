import 'package:flutter/material.dart';

class ButtonBaseWidget extends StatelessWidget {
  const ButtonBaseWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
